node_modules
npm-debug.log
.git
.gitignore
# Never bake secrets into the image — provide them at runtime via env vars
# (DATABASE_URL, JWT_SECRET) and FIREBASE_SERVICE_ACCOUNT_JSON for the key.
.env
.env.*
firebase/
docs
Dockerfile
.dockerignore
docker-compose.yml
